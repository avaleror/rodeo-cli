# rodeo-cli audit — 2026-08-05 (docs / structure)

Docs-only re-audit after v0.14.2. No runtime or Ansible behavior changes in this pass.
Prior snapshots: [2026-07-14](audit-2026-07-14.md), [2026-07-16](audit-2026-07-16.md).

**Repo state:** `main` @ **0.14.2** · **420** tests collected · ruff pin `0.15.16` · license GPL-3.0-or-later.

---

## What this pass fixed (docs / hygiene)

| Area | Action |
|------|--------|
| Stale version / test / Instruqt claims | Synced `CONTEXT.md`, site hero, guides, plan reference |
| Profile tables | `test` = 2-node; `harvester-2n` ≈ 56 GiB; doctor vs lab-footprint note in Harvester guide |
| README commands | Documented `fleet`, `destroy`, day-2 commands |
| ROADMAP | Added **Next up**; corrected B2 intro for opt-in `--reconcile`; PXE test-gap note |
| Structure | Audits under `docs/archive/`; broader `.gitignore` secrets/editor ignores |

---

## Deferred (product / tests — not in this pass)

| Item | Notes |
|------|--------|
| PXE network structural test | ✅ `tests/test_pxe_network_idempotency.py` |
| Harvester seed-ISO rebuild | ✅ Documented intentional (same as rancher/eib) |
| B2 step 5 | Live-validate `--reconcile`, then default on |
| B1 ownership tags | Prerequisite for precise `destroy` / `list` |
| Phase H Level 1 | Hauler prefetch |
| F4b GCP | After AWS F4a |
| `suse-edge` Instruqt | Bare-metal OK; track pending |
| `rancher.py` split | Maintainability only |

---

## Idempotency (review only)

- virbr0 destroy/redefine guarded on both vms and pxe paths (per-node DHCP marker).
- `no_cache_phases` includes `apply` on all three engines.
- Opt-in `--reconcile` covers VM memory/vCPU + NAT DHCP host markers.
- Seed ISO rebuilds on every `vms` re-run remain intentional (documented on rancher/eib; Harvester config ISO same pattern).
