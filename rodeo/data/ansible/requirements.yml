---
collections:
  - name: community.general
    version: ">=8.0.0"
  # 1.x is bugfix-only through ~2026-07; pin 2.x (virt/virt_net/virt_pool unchanged).
  - name: community.libvirt
    version: ">=2.0.0,<3"
  - name: ansible.posix
    version: ">=1.5.0"   # firewalld port_forward/toaddr (DNAT) needs >=1.4.0
