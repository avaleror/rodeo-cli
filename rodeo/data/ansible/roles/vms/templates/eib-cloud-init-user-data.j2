#cloud-config
hostname: eib
disable_root: false
users:
  - name: root
    lock_passwd: false
    ssh_authorized_keys:
      - {{ ssh_public_key }}
chpasswd:
  list: |
    root:{{ rancher_vm_password }}
  expire: false
ssh_pwauth: true
